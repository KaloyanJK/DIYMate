from django.apps import AppConfig


# Configure the Planner application settings for Django
class PlannerConfig(AppConfig):
    # Specify the application name registered with Django
    name = 'planner'
